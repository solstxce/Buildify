# Gradle / Android build
.gradle/
**/build/
.cxx/
local.properties

# Captures / profiling
captures/

# IDE
*.iml
.idea/

# Keystore — never commit signing keys
key.properties
**/*.keystore
**/*.jks

# Keep Gradle wrapper in Git so clones can build without a global Gradle install.
# (Do NOT ignore gradlew, gradlew.bat, or gradle/wrapper/*.jar)
